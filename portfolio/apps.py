from django.apps import AppConfig


class PortfolioConfig(AppConfig):
    """Portfolio app: gallery of past organisations."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'portfolio'
    verbose_name = 'Portfolyo / Galeri'
